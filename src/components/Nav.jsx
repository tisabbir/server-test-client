import { NavLink } from "react-router-dom";


const Nav = () => {
    return (
        <div className="">
            <NavLink to={'/phones'}>Phones</NavLink>
        </div>
    );
};

export default Nav;